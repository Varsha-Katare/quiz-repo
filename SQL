CREATE DATABASE code_gauntlet;
CREATE TABLE users (
  id SERIAL PRIMARY KEY,
  name TEXT,
  score INT DEFAULT 0
);

CREATE TABLE submissions (
  id SERIAL PRIMARY KEY,
  user_id INT,
  answers JSONB,
  score INT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);